import { randomRange } from '../helpers';
import { getSpriteByIndex } from 'core/helpers/sprites';

export default {
  walkable: true,
  layer: 'objects',
  texture: 'layer1',
  sprites: {
    size: [168, 207],
    location: [412, 0]
  }
}
